{
  "name": "deepseek-harness",
  "version": "0.1.5",
  "private": true,
  "description": "DeepSeek-Harness — Tauri desktop shell embedding the DeepSeek Harness web UI",
  "scripts": {
    "icon": "node scripts/make-ico.mjs",
    "dev": "tauri dev",
    "build": "node scripts/build.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "sharp": "^0.35.3"
  }
}
